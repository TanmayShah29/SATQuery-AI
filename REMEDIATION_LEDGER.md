# SatQuery AI (SIH26167) — Remediation Ledger

**Source of truth:** `AUDIT_REPORT.md` (17 Sep 2026, verdict CONDITIONAL / NOT SHIP-READY).
**Deadline:** 30 Sep 2026.
**Rule:** Every finding must end as `FIXED` with a reproducible verification, or `WONT-FIX-BECAUSE` with an honest fallback/disclosure documented. No finding is closed by "code was written" — only by a run that demonstrates the fix.

Row format: `| Finding | Phase | Status | Owner | Fix (file:line) | Verification method used | Verified by |`

Verification protocol (all runs):
- Backend units run directly under `.agents/venv/bin/python` (not via the live `--reload` server).
- Cross-sector probes use the exact audit queries (joshimath-subsidence vs malacca-chokepoint, `"Has there been surface subsidence in this area?"`).
- A fix is `FIXED` only when two genuinely different inputs produce explainably different (or explicitly-identical-with-disclosure) outputs.

---

## Status table

| # | Finding | Phase | Status | Owner | Fix (file:line) | Verification method used | Verified by |
|---|---------|-------|--------|-------|-----------------|--------------------------|-------------|
| 1 | A-1 `D1` Template answer substitutes genuine RemoteCLIP output whenever Ollama is down | A | FIXED | Agent 1 | `realtime_vlm_agent.py:generate_vlm_answer` returns `(None,None,None,None)` when Ollama down; template fn + all scripted prose deleted | venv probe joshimath+malacca with Ollama offline: answer = real DSP text, no template | Self (17 Sep) |
| 2 | A-2 `D1` Hardcoded confidence constants (clamp `0.72–0.95`, literals `0.91/0.89/0.86/0.87/0.93`) | A | FIXED | Agent 1 | `realtime_vlm_agent.py` rewrite — no clamp/literals; confidence adopted only `if vlm_conf is not None` (evidence_builder PATH1-3) | grep: no `0.91/0.89/0.86/0.87/0.93/0.72/0.95`; probe confidence real (0.945 Otsu / 0.72 SAR) | Self (17 Sep) |
| 3 | A-3 `D1` Model mislabeled as "RemoteCLIP-ViT Neural VLM" for rule-based template | A | FIXED | Agent 1 | template removed; `active_model_name` = deployed `RemoteCLIP-ViT-B/32 Domain-Adapted Neural VLM`; DSP paths report `ai_engine_active` honestly | probe `ai_engine_active` = `bitemporal_change_dsp` / `crossmodal_sar_fusion_dsp` (not `remoteclip_vlm`) | Self (17 Sep) |
| 4 | A-4 `D2` Answer asserts Sentinel-1 DInSAR / interferometric displacement with no implementation | A | FIXED | Agent 1 | template removed; DInSAR sensor string removed (SA-A); prompt now states "There is NO InSAR/DInSAR interferometric processing" | full sweep: `grep -riE 'dinsar\|insar\|sbas\|interferometr' backend/app` → only the 2 negative-prompt lines; live answers for 5 sectors (`joshimath`, `malacca`, `galwan`, `pokhran`, `brahmaputra`) all clean | Agent 11 (17 Sep) |
| 5 | A-5 `D2` Same fabricated answer regardless of geography (twins from D4) | A | FIXED | Agent 1 + SA-A | real per-scene computation + sector name/bbox/imagery_origin; distinct audit_hash | probe joshimath vs malacca: different answer text + `SHA256-7879…` vs `SHA256-E0D8…` (direct) | Self (17 Sep) |
| 6 | A-6 `D1` DAG/telemetry timing contradicts real latency (144 ms claim) | A | FIXED | Agent 2 | `evidence_builder.py:400-441` real `perf_counter` timings; vectorizer `None` when not separately timed | probe latencies: node3 22.6–54.3 ms measured, node4 None (SAR/bitemporal) | Self (17 Sep) |
| 7 | B-1 `D3` Non-georeferenced JPEGs claim "EPSG:4326 / GSD 10m" + "georeferenced" GeoJSON | A | FIXED | Agent 2 (+SA-A) | `evidence_builder.py:113-127,419` raster_desc/georef_label/geojson_note from real `meta`/profile; no hardcoded CRS/GSD | probe node2 = `Imagery origin: sector_files:… | Georeferenced: False`; node4 approximate-polygon note | Self (17 Sep) |
| 8 | B-2 `D4` isro-sac / custom-aoi / joshimath share byte-identical rasters yet answer as own geology | A | FIXED | Agent 11 | `sector_assets.py` `_sector_fingerprint_map`/`_sector_imagery_group` detect **cross-sector** byte sharing (excluding the real `isro-sac` branch); `imagery_distinct` now means "unique among all sectors"; shared sectors report `shared_demo_raster[custom-aoi,joshimath-subsidence]`. `evidence_builder.py` step 8 prepends a visible `SIMULATED DEMO RASTER — not satellite acquisition` disclosure to the answer + `data_provenance` for simulated sectors | direct venv probe: joshimath & custom-aoi → `distinct=False` + disclosure prefix; malacca → `distinct=True` + disclosure; isro-sac → `real=True`, no disclosure; `pytest` 56 passed (incl. 2 new guards) | Agent 11 (17 Sep) |
| 9 | B-3 `D3` Sector provenance not carried into answer narrative / DAG | A | FIXED | Agent 2 | `evidence_builder.py:113-127,419` + telemetry `imagery_origin/imagery_distinct/raster_is_georeferenced/data_provenance` | probe node2 carries origin+distinct+georef; telemetry carries full provenance block | Self (17 Sep) |
| 10 | C-1 `D5` `live_satellite_stream` always `None`; `fetch_live_satellite_scene` has zero callers | A | FIXED | Agent 5 + Agent 9 | `evidence_builder.py` opt-in STAC fetch gated on `context_hints.live_stream`, honest `telemetry.live_stream_status`; `realtime_vlm_agent.py` maps `datetime`→`acquisition_date`, strips `pil_image`; **UI**: `App.tsx` `liveStreamEnabled` state + `QueryPromptBar` "LIVE STAC ON/OFF" toggle → `contextHints:{live_stream}` | backend probe default `None`/`not_requested`; opt-in fetches real `S2A_44RLU_20240619_0_L2A`; frontend `tsc --noEmit` + `vite build` clean; grep confirms `contextHints` wiring | Self (17 Sep) |
| 11 | C-2 `D5` Hardcoded `online` statuses (AWS STAC / Sentinel-1 / ESRI) + UI "100% ONLINE"/"100% REAL SCENES" | A | FIXED | Agent 1 + Agent 9 | `get_ai_status` real HTTP probes + ollama `/api/tags` + weights check; `ModelStatusModal.tsx` fallback feed array deleted (renders probed feeds only + honest empty state), "100% REAL SCENES"→"STATUS PROBED LIVE", model status pill colour/label real, VRSBench/CDVQA claims removed; `RightOperationalPanel` STAC badge from real `stacStatus`; `Landing.tsx` "4 ORBITERS ONLINE"→"MULTI-ORBITER STAC QUERY" | backend probe: AWS/PC/ESRI `online`, Bhuvan/MOSDAC `not_configured`, ollama `offline`; `tsc --noEmit` + `vite build` clean | Self (17 Sep) |
| 12 | D-5a `sector_assets.py:406` per-process-random `hash()` seed → non-deterministic timeline | B | FIXED | SA-A | `sector_assets.py` stable `zlib.crc32` seed | SA-A: repeated calls stable timeline; probe audit_hash deterministic per sector | SA-A subagent (17 Sep) |
| 13 | D-5b `geotiff_parser.py:100-101` unknown-CRS → hardcoded `gsd_meters=10.0` | B | FIXED | SA-A | `geotiff_parser.py` unknown-CRS → `gsd_meters=None` + image-space note | SA-A: unknown-CRS raster yields `gsd_meters: null`, no 10.0 | SA-A subagent (17 Sep) |
| 14 | D-5c `sar_fusion.py:217` fixed `0.82` when no features detected | B | FIXED | SA-A | `sar_fusion.py` no-feature path → `confidence=None`, `detection_quality:"no_high_backscatter_targets_detected"` | SA-A: no-feature call returns confidence None, not 0.82 | SA-A subagent (17 Sep) |
| 15 | E-1 `D7` 4-byte garbage `.tif` accepted by `/api/upload` as valid raster | A | FIXED | SA-B | `routes_upload.py:_validate_raster_content` magic-byte + min-size | SA-B: 4-byte garbage → HTTP 400; valid TIFF → accepted | SA-B subagent (17 Sep) |
| 16 | C-3 (P2) `activeAlertsCount={4}` static, no backend feed | C | FIXED | Agent 9 | dead prop removed from `HeaderHUD.tsx` + `App.tsx` (was declared/defaulted but never rendered); drawer alert tab banner now states "static demo content, not live measurements" | grep: no `activeAlertsCount`; `tsc --noEmit` clean | Self (17 Sep) |
| 17 | D-1 (P1) registry "800k+ Remote Sensing Image-Caption Pairs" unverifiable | B | FIXED | Agent 6 | `registry.py:22` provenance = backbone + "84 real samples (58/26)"; VRSBench fine-tune claims removed (`:32`,`:42`); vectorizer CRS claim corrected (`:82`) | grep: no `800k+`/VRSBench fine-tune; probe registry strings honest | Self (17 Sep) |
| 18 | D-2 (P1) training metrics: train top-1 < eval top-1; `512->256->512` doc vs `512->256` code | B | FIXED | Agent 6 | `training_metrics.json` dataset string de-fabricated + `small_sample_caveat` + `adapter_architecture_code_verified`; code confirms 512→256→512 | probe: adapter param count computed = `528897` == metrics; JSON valid; caveat present | Self (17 Sep) |
| 19 | D-3 (P2) `remote_clip.py:419` nudge + `:526` fallback class/0.85 | C | FIXED | Agent 6 | per-feature `confidence` now image-level posterior + `confidence_basis`; `:526` fabricated class/0.85 replaced with `RuntimeError` (no silent fallback) | probe: feature conf = 0.342 real posterior (was 0.80 nudge), basis `image_class_posterior` | Self (17 Sep) |
| 20 | D-4 (P2) hardcoded spectral/cloud/dB stand-ins and latencies feeding VLM inputs | C | FIXED | Agent 1 + Agent 2 | `evidence_builder.py` real `compute_spectral_indices`; SAR dB None default; cloud = `profile.typical_cloud_pct` labeled nominal; latencies real | probe: spectral/albedo/NDVI from pixels; SAR `mean_backscatter_db` real or n/a | Self (17 Sep) |
| 21 | E-2 (P2) GeoJSON branch swallows parse errors; default bbox stand-in | C | FIXED | SA-B | `routes_upload.py` GeoJSON parse error → HTTP 400; `inspect_pixel_point` requires explicit bbox | SA-B: malformed GeoJSON → 400; no silent default bbox | SA-B subagent (17 Sep) |
| 22 | 3F `test_api.py:66-70,91` enshrine clamp + mislabeled telemetry; `test_integration` blesses minimal TIFF; no 4-byte test | B | FIXED | Agent 8 | `test_api.py` clamps → honest `[0,1]` + `ai_engine_active` asserts; `sar_backscatter_db` → honest `_proxy_db` + note; +6 guards (4-byte 400, distinct-hash, no-InSAR, provenance block, live-stream opt-in, probed ai-status) | pytest: 54 passed (48 prior + 6 new) | Self (17 Sep) |
| 23 | 3G `ModelStatusModal.tsx:82-86,321` "100% REAL SCENES" + hardcoded online | A | FIXED | Agent 9 | fallback feed array deleted (probed data only + honest empty state); status pill conditional (green only when real); "100% REAL SCENES"→"STATUS PROBED LIVE"; "Fine-tuned on …VRSBench"→"RS backbone + BigEarthNet adapter, 84 real samples"; "CDVQA Benchmark" removed | `tsc --noEmit` + `vite build` clean | Self (17 Sep) |
| 24 | 3G `SituationalFeedDrawer.tsx:88-92` hardcoded ACTIVE/ONLINE incl. MOSDAC | A | FIXED | Agent 9 | all pass statuses prefixed `SIM ·` + neutral blue pill (no live assertion); alert presets de-fabricated (`+InSAR`→optical, `+1.4°C`/`94%`/`14.8 km²` removed) + banner "static demo content, not live measurements" | `tsc --noEmit` + `vite build` clean; grep: no `InSAR`/`14.8 km²` in drawer | Self (17 Sep) |
| 25 | 3G `RightOperationalPanel.tsx:194-211,503,581,799` mislabeled PRIMARY_MODEL + static ONLINE pill + dead stream card | A | FIXED | Agent 9 | report `PRIMARY_MODEL`/`AUDIT_HASH` honest `UNAVAILABLE`, confidence/latency null-safe; STAC badge derives from real `stacStatus` (ONLINE/NO SCENES/UNREACHABLE/IDLE); `acquisition_date` null-safe; "RemoteCLIP Cross-Verification"→"Model-reported (no external benchmark)"; "100% (All-Weather)"→"Cloud-independent (C-band)" | `tsc --noEmit` + `vite build` clean | Self (17 Sep) |
| 26 | 3G `tacticalData.ts:23,302` duplicate isro-sac pins (P2) | C | FIXED | Agent 9 | `SECTOR_TARGETS` isro-sac entry now spread-derived from the single `TACTICAL_PINS` pin; fabricated `temporalDelta`/`activePasses`/`state:'OPERATIONAL'` neutralized | `tsc --noEmit` clean; grep: one isro-sac definition | Self (17 Sep) |
| 27 | 3H Justify: real RemoteCLIP cos-sim, DSP engines, honest telemetry+ISO badges | — | WONT-FIX-BECAUSE | — | preserve as-is | — | — |
| 28 | New (Agent 11) `evidence_builder.py:87` joins client `uploaded_file` into uploads path without confinement → arbitrary-file read / 500 | — | FIXED | Agent 11 | confine to `uploads/`: join only `Path(uploaded_file).name` and require `is_relative_to(uploads_root)`; traversal falls back to sector | live HTTP: query with `uploaded_file="../../../../etc/passwd"` → 200, telemetry `sector_id` = requested sector; `test_query_ignores_traversal_uploaded_file` | Agent 11 (17 Sep) |
| 29 | New (Agent 11 + user follow-up) `routes_upload.py` persisted a rejected file (**both** branches) before validation → orphaned files + evidence on HTTP 400 | — | FIXED | Agent 11 | GeoJSON: validate/parse in memory **before** write. Raster: `_reject_raster()` removes the written file + `_thumb.jpg` on parse failure, and `parse_raster` returning `validation_passed=False` (magic-valid but unreadable: truncated/corrupt IFD/unsupported compression) is now also rejected 400 instead of 200 | `test_invalid_geojson_leaves_no_orphan_file` + `test_corrupt_magic_valid_tiff_rejected_without_orphan` assert uploads dir unchanged; live HTTP corrupt 64-byte `.tif` → 400, uploads count 49→49; `pytest` 59 passed | Agent 11 (17 Sep) |
| 30 | 4d (user-flagged) `geotiff_parser.py` fabricated `"EPSG:4326 (Unspecified GeoTIFF)"` for no-CRS rasters, publishing affine/pixel-unit bounds as WGS84 + a bogus GSD; `evidence_builder.py` consumed `is_geotiff` as if it meant georeferenced | — | FIXED | Agent 11 | `geotiff_parser.py`: `is_georeferenced = src.crs is not None`; no CRS → `crs="Unspecified (no CRS in file)"`, `bounds=None`, `gsd_meters=None` + validation note; GSD branch gated on `crs_known`. `evidence_builder.py`: use `is_georeferenced`; when bounds None keep demo AOI but set `extent_verified=False` → telemetry `geographic_extent_verified=False`, `bounds_evaluated=None` | live HTTP no-CRS TIFF → `crs: Unspecified (no CRS in file)`, `bbox: null`, `gsd_meters: null`; query on it → `raster_is_georeferenced:false`, `geographic_extent_verified:false`, `bounds_evaluated:null`; `pytest` 59 passed | Agent 11 (17 Sep) |
| 31 | Mission: the agent could describe the map but never touch it — answers claimed UI state that no code applied | M | FIXED | Agent 2 + Agent 3 | Backend `ui_action_planner.py` emits a closed 14-type grounded `UIAction[]` + `node-5-ui-actuation` (`pending`); frontend `actionExecutor.ts` resolves each against the real handle/stores and returns a true `ExecutionReport`, folded back into the DAG node. Actions that cannot execute report `failed`/`skipped` with the real reason — never a fabricated confirmation | `verify_agent3_action_executor.mjs` **9/0/0**: highlight filter `["==",["id"],"change-feat-1"]` applied by the executor, camera entered the sector (`79.564,30.558`), DAG `5/5` with `6/6 grounded UI action(s) executed`, payload names the proposed types, crossmodal click-through flips the highlight to `sar-target-1` + `set_sensor_band` | Agent 3 (17 Sep) |
| 32 | Mission blocker: pins/evidence layers were attached **only** inside `map.on('load')`, so an unreachable basemap tile source (observed: `isStyleLoaded()===false` forever, 4 style layers) left the entire operational stack absent → every agent `highlight_feature` correctly reported `failed` | M | FIXED | Agent 3 | `MapCanvas.tsx`: extracted the init body to an idempotent `initOperationalLayers()` bound to **both** `load` and `styledata` (guard `operationalLayersReady`, `getStyle()` check), matching the ready-handler pattern already used for viewport sync | live headless probe before fix: `hasEvidence:false`; after: `layers 4→9`, `hasEvidence:true`, executor filter applies; `verify_agent1_map_controls.mjs` 12/0/0 no regression | Agent 3 (17 Sep) |
| 33 | Mission correctness: each sector/modality activation issued the query **twice** (direct handler + synchronizer effect). Observed 5–7 `/api/query` calls per mount; responses returned out of order and each one re-actuated the map, so the camera/highlight could settle on a request nobody made | M | FIXED | Agent 3 | `App.tsx`: `inFlightQueriesRef` collapses concurrent identical `sector\|modality\|text` requests; `lastAnalyzedSectorRef`/`lastAnalyzedPinRef` are now written by `executeQuery` itself so no caller can double-issue | live request trace collapsed to one query per pair; harness green with deterministic camera/highlight | Agent 3 (17 Sep) |
| 34 | Mission correctness: deep link `?sector=<id>` was analysed as the **default** pin first (hydration lands one commit later); the default response then flew the map back to its own sector | M | FIXED | Agent 3 | `App.tsx` synchronizer returns early while `urlState.sector !== selectedPin.id`, so no query is issued until URL sector hydration settles | `/tactical?sector=joshimath-subsidence` → exactly one analysis, DAG target = Joshimath, camera stays on sector | Agent 3 (17 Sep) |
| 35 | Mission correctness: a modality switch re-ran the sector's **default prompt** instead of the question on screen, so the deck (and the agent's map actions) answered a different question | M | FIXED | Agent 3 | synchronizer reuses `queryResponse.query` whenever only the modality changed (falls back to `recommendedQuery` on sector change or when there is no prior response) | modality toggle from the deep-linked state re-runs the active query text; no more contradictory evidence | Agent 3 (17 Sep) |
| 36 | 3G (mission-adjacent) `QueryPromptBar.tsx:77,412` forwarded the current modality, bypassing App intent detection → "SAR radar pierce monsoon cloud" was analysed as `bitemporal_change` and missed the SAR evidence entirely | — | FIXED | Agent 3 | forward the **text only**; App resolves modality from the words (`sar/radar/cloud/monsoon/pierce` → `cross_modal`, `change/between/t1/t2/delta` → `bitemporal`) and falls back to the active toggle for neutral text | probe of the same text: `modality=bitemporal` before → `cross_modal`/`sar-target-1` after; harness crossmodal assertion now passes | Agent 3 (17 Sep) |

---

## Execution log (append chronological entries)

| Timestamp | Action | Result |
| 17 Sep (Agent 3, mission) | Agent-controlled map/dashboard: backend grounded `UIAction[]` + frontend executor; fixed the four real defects the browser harness exposed (evidence layers gated behind `map.on('load')`, duplicate queries, deep-link override, modality-switch wrong prompt) and the prompt-bar modality bypass | Rows 31-36 FIXED; `verify_agent3_action_executor.mjs` **9/0/0**; `verify_agent1_map_controls.mjs` 12/0/0 no regression; `pytest` 79 passed; `tsc --noEmit` + `vite build` clean |
|-----------|--------|--------|
| 17 Sep (P0) | SA-A: provenance fields, stable seed, DInSAR sensor removal, unknown-CRS GSD null, SAR no-feature confidence null | Rows 12,13,14 FIXED (verified by SA-A subagent); row 4 partial (sensor string) |
| 17 Sep (P0) | SA-B: upload magic-byte/min-size validation, GeoJSON 400, explicit-bbox requirement | Rows 15,21 FIXED (verified by SA-B subagent) |
| 17 Sep (Agent 1) | `realtime_vlm_agent.py` rewritten: template synthesis deleted; `generate_vlm_answer` honest (None when Ollama down); real `get_ai_status` probes; provenance + anti-InSAR prompt | Rows 1,2,3 FIXED; row 11 backend done |
| 17 Sep (Agent 2) | `evidence_builder.py`: honest defaults, per-intent `engine_active`, `if profile:` synthesis gates, real `compute_spectral_indices`, honest node2/node4 payloads + geojson note, benchmarkSource per intent, provenance telemetry, audit_hash + sector/origin | Rows 5,6,7,9,20 FIXED; rows 4,8 IN-PROGRESS; row 11 backend done |
| 17 Sep (verify) | `py_compile` + import OK both files; venv probe joshimath vs malacca: hashes differ (`7879665D…` vs `E0D87AD2…`), no DInSAR, engines DSP, latencies real; live `/api/query` on refreshed 8080 server matches | Rows 5,6,7 confirmed against live HTTP; server was stale (no `--reload`) and restarted with `--reload` |
| 17 Sep (Agent 6) | `registry.py` honest provenance; `training_metrics.json` de-fabricated + small-sample caveat; `remote_clip.py` per-feature confidence = real image posterior + `confidence_basis`, text-tower fallback raises | Rows 17,18,19 FIXED; probe params `528897` match, feature conf 0.342 real |
| 17 Sep (Agent 5) | Opt-in live STAC scene wired into query envelope: real fetch when `context_hints.live_stream=true`, honest `not_requested` default, `acquisition_date` mapped, `pil_image` stripped | Row 10 backend done (UI toggle pending Agent 9); probe real Sentinel-2 scene fetched |
| 17 Sep (Agent 8) | Test suite de-clamped: honest confidence ranges + `ai_engine_active`, proxy-labelled SAR index, 6 new anti-fabrication guards | Row 22 FIXED; `pytest backend/tests` = 54 passed |
| 17 Sep (Agent 9) | Frontend truth-pass: probed-only stream feeds + honest empty state, `SIM ·` pass statuses, de-fabricated alert presets, real STAC badge state, null-safe report/telemetry fields, single-source `isro-sac` pin, dead `activeAlertsCount` removed, LIVE STAC opt-in toggle wired to `context_hints.live_stream` | Rows 10,11,16,23,24,25,26 FIXED; `tsc --noEmit` + `vite build` clean |
| 17 Sep (Agent 11 pre-pass) | Closed the last two IN-PROGRESS findings: InSAR full-sweep clean (row 4); cross-sector raster-sharing detection + visible simulation disclosure (row 8) | Rows 4,8 FIXED; `pytest` 56 passed; ledger has **no OPEN/IN-PROGRESS rows** |
| 17 Sep (Agent 11, independent re-audit) | Dispatched an independent subagent that treated the ledger as falsifiable and re-ran every §3 probe (22 greps, md5 sweep, 6 live HTTP probes, frontend build, pytest). Result **20/20 CONFIRMED FIXED, 0 still-broken, 0 ledger overclaims**; report `INDEPENDENT_REAUDIT_2026-09-17.md` | All rows re-confirmed; audit hash independence re-proven |
| 17 Sep (Agent 11, residuals) | Fixed the two Medium residuals the re-audit surfaced: traversal confinement in `evidence_builder.py`; validate-before-persist in `routes_upload.py` | Rows 28,29 FIXED; `pytest` 58 passed (2 new guards); garbage `file_00.php_*.tif` probe artifacts removed |
| 17 Sep (Agent 11, user follow-up) | User caught that row 29's raster sibling branch was untested: `parse_raster` does not raise on magic-valid-but-unreadable TIFFs (it degrades to `validation_passed=False`), so a corrupt raster returned HTTP 200 and persisted. Added `_reject_raster()` cleanup + `validation_passed` gate → 400; new `test_corrupt_magic_valid_tiff_rejected_without_orphan` | Row 29 scope corrected (both branches); live corrupt `.tif` → 400 with uploads dir unchanged; `pytest` 59 passed |
| 17 Sep (Agent 11, §4.d) | No-CRS GeoTIFF honesty: removed fabricated `EPSG:4326 (Unspecified GeoTIFF)`, nulled bounds/GSD, wired `is_georeferenced` through upload + query telemetry (`geographic_extent_verified`, `bounds_evaluated`) | Row 30 FIXED; live no-CRS upload + query honest; `pytest` 59 passed |