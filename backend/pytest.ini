[pytest]
testpaths = tests
pythonpath = .
markers =
    integration: marks tests as integration tests requiring Ollama and full stack (deselect with '-m "not integration"')
    slow: marks slow tests
addopts = -v --tb=short
